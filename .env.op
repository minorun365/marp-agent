# 1Password secret references — run `op-sync` to generate .env
# Non-secret config values
VITE_USE_MOCK=false
AMPLIFY_DIFF_DEPLOY=true
AMPLIFY_DIFF_DEPLOY_ROOT=src

# Secrets (resolved from 1Password Private vault)
TAVILY_API_KEYS=op://Private/marp-agent/TAVILY_API_KEYS
TEST_USER_EMAIL=op://Private/marp-agent/TEST_USER_EMAIL
TEST_USER_PASSWORD=op://Private/marp-agent/TEST_USER_PASSWORD
